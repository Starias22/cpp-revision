#include"../header/fraction.hpp"

//int main(){
  //fraction l(1,0);
    /*fraction f(1,2);
    f.displayn();
    f=fraction(10,5);
    f.displayn();
    fraction g= fraction(2,6);
    
     g.displayn();
    g/=2;
    g.displayn();
    
    g.displayn();
  cout<<(g==f)<<endl;
  cout<<(g>f)<<endl;
  g=f.inverse();
  g.displayn();
  f=(4);
  f.displayn();
  f/=3;
  f.displayn();
  f=fraction(10,5,true);
  f.displayn();
  f=14/2;
   f.displayn();
   f=fraction(15,3)+fraction(1,3)*2-1;
   f.displayn();*/
   //int c[10];
   
    //fraction tab[10] ;
    //fraction h(int num=1,int den=2,char c='h');
    
   
/*create a table and initialize each element using 
the constructor without parameter*/
  //fraction tab2[10]{} ;fraction tab3[10];fraction tab4[10]={} ; 
  //tab2[0].displayn();
/*create a table and initialize  the first element with the constructoor 
with 2 parameters and the others elements using the constructor
 without parameter*/
  //fraction tab[10]{fraction(2,3)} ;
  //tab[0].displayn();
  //tab[1].displayn();

  /*for(fraction i:tab)
  i.displayn();*/

/*cout<<"Elements of tab\n";
  for(fraction i:tab)
  i.displayn();*/
  /*fraction matrix[10][10];

  matrix[0][0].displayn();
  matrix[0][1].displayn();*/
  //fraction k[10];
  //k[0]=fraction(100,20);
  //k[0].displayn();
  //fraction x=fraction(4,5);
  //fraction y;

  //fraction *ptr=new fraction();
 // ptr[0]="2/6";
//  ptr->displayn(true);
//  delete ptr;

  //fraction tab[3][3];

  //fraction *p=new fraction();
  //fraction **ptr=new fraction*();
  //delete *ptr;
  //fraction *x=new fraction[10][10];

  /*fraction **ptr=new fraction*[10];
  for(int i=0;i<10;i++)
  *(ptr+i)=new fraction();
   delete *ptr;*/
   //matrix matrix();
  
   // return 0;
//}                               