#include <iostream>
int main()
{
    //+-*/ %()! || &&  == > < >= <= += -= ++ -- *= /= %=
bool test=1,a;
a=test==true;
std::cout<<a;
int x=10;
std::cout<<x;
x++;//x+=1
std::cout<<x;
--x;
std::cout<<x;
 x %= 10;

    return 0;
}