#include<iostream>
#include<algorithm>
using namespace std;

void replace(string src,string old,string nw);
/*auto search(string src,
string::iterator it=0,
string sch);*/



using namespace std;

void replace(string src,string old,string nw);

int main()

{
    int tab[10]={};
    //min(begin)
    return 0;
}

void replace(string src,string old,string nw){
//src.replace(src.begin(),);
//replace(src.begin(),src.end(),old);
    string::iterator it;
    //it=find(src.begin(),src.end(),)
}

/*auto search(string str,string::iterator it,string sch)
{
    auto it=find(str.begin(),str.end(),sch[0]);
    if(it !=str.end()&&
    (str.substr(it-str.begin(),sch.length())==sch))
    return it+str.length();



}/
